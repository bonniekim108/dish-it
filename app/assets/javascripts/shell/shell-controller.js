(function() {
  'use strict';

  angular.module('app')

  .controller('ShellController', [
    '$state',
    '$rootScope',
    // 'currentUser',
    function($state, $rootScope) {
      var self = this;

      // $rootScope.currentUser = currentUser;

    }
  ]);


})();